name: Run every 1 hour

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  cron:
    name: Run every 1 hour
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        run: echo "Hello World"
